info:
  name: refreshToken
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/auth/refresh-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

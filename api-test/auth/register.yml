info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Akrawin Datchanee",
        "email": "Onlyakr2477@gmail.com",
        "password": "Only1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

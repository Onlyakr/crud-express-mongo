info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "only",
        "email": "only@email.com",
        "asdf": "asdfsadf",
        "password": "only1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

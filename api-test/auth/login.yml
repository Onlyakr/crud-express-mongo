info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "Onlyakr2477@gmail.com",
        "password": "Only1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

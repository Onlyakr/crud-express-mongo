info:
  name: createPost
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/posts"
  body:
    type: json
    data: |-
      {
        "title": "My thrid post",
        "content": "This is my third post.",
        "image": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: deletePost
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseURL}}/posts/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

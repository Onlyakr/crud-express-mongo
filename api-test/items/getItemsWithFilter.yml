info:
  name: getItemsWithFilter
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/items?name=Bag"
  params:
    - name: name
      value: Bag
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

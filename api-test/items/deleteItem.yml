info:
  name: deleteItem
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/items/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
